// Indexação
// First(), last(), String.length, String[index];

val s = "Olá, Mundo!"
println(s[0])
println(s.first())

println(s[s.length() - 1])
println(s.last())

val name = "Ana"
val s = "Olá"

println("Bem vindo(a), $name!")

// Capitalização de strings
// capitalize(), toUpperCase(), toLowerCase(). and decapitalize()

// Remoção de espaços
// trim(), trimStart(), trimEnd().

// Substituição de caracteres
// replace(x, y)

// Formatação
// "padrão ${valor}".format(valor)